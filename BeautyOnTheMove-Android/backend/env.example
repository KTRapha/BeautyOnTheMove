# Backend Environment Configuration
NODE_ENV=production
PORT=3000

# JWT Configuration
JWT_SECRET=beautyonmove_jwt_secret_key_2025
JWT_EXPIRES_IN=7d

# Database Configuration (for future MongoDB integration)
MONGODB_URI=mongodb://localhost:27017/beautyonmove

# Email Configuration (for future email features)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# AWS Configuration
AWS_REGION=us-east-1
AWS_S3_BUCKET=beautyonmove-app-assets

# Security
CORS_ORIGIN=*
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# App Configuration
APP_NAME=BeautyOnTheMove
APP_VERSION=1.0.0 